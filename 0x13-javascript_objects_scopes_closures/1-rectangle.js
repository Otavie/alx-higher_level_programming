#!/usr/bin/node

class Rectangle {
  // This is a class that defines a rectangle with width and height
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
}

module.exports = Rectangle;
